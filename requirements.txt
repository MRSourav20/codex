# Optimus – Monitoring Platform Dependencies

# Core
python-dotenv==1.0.0
requests==2.31.0

# DNS Capture
pyshark==0.6

# Terminal Dashboard
rich>=13.0.0
textual>=0.40.0

# Web (optional – only if API endpoint needed)
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# Optional
opencv-python-headless>=4.8.0  # for facial monitoring (skip if not needed)
